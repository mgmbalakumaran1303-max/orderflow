# ORDERFLOW

Restaurant Admin Portal.

## Run

```bash
npm install
npm run dev
```

Sign in with `admin@restaurant.com` / `password123`.

Use `offline@restaurant.com` to preview the login network error state.
